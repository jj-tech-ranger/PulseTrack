import 'package:flutter/material.dart';

// Navy and blue shades
const Color primaryNavy = Color(0xFF1E3A8A);
const Color darkNavy = Color(0xFF0F172A);
const Color pastelBlue = Color(0xFFBAE6FD);

// Background and text
const Color backgroundColor = darkNavy;       // Dark theme background
const Color textPrimary = Colors.white;       // Primary text is white
const Color textSecondary = Colors.grey;      // Secondary text is grey
const Color cardColor = primaryNavy;          // Example: cards use primary navy
